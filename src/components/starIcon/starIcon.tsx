// vendors
import React from 'react';
// styles
import { StarIconWrapper, IconStarStyled } from './starIcon.styled';

const StarIcon = (): React.ReactElement => {
  return (
    <StarIconWrapper>
      <IconStarStyled />
    </StarIconWrapper>
  );
};

export default StarIcon;
